class Solution:
    def twoSum(self, numbers, target):

        start = 0

        while True:
            for i in range(1,len(numbers)):
                if start == i:
                    continue

                if numbers[start] + numbers[i] == target:
                    return [start+1,i+1]
            start +=1
