 
class Solution:
    def removeDuplicates(self, nums) -> int:

        start = 0

        for x in range(len(nums)):
            if nums[x] != nums[start]:
                start += 1
                nums[start] = nums[x]
        return start + 1
