class Solution:
    def sortArrayByParity(self, nums):

        evens = []
        odds = []

        for x in nums:
            if x % 2 != 1:
                evens.append(x)
            else:
                odds.append(x)


        return evens + odds
