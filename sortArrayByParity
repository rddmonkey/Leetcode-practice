class Solution:
    def sortArrayByParity(self, nums):

        ans = []

        for x in nums:
            if x % 2 != 1:
                ans.append(x)

        for x in nums:
            if x % 2 == 1:
                ans.append(x)

        return ans
