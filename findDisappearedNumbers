class Solution:
    def findDisappearedNumbers(self, nums):

        mydict = {}

        ans = []

        for x in nums:
            if x not in mydict:
                mydict[x] = 1


        for i in range(1,len(nums)+1):
            if i not in mydict.keys():
                ans.append(i)

        return ans
