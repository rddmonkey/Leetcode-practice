class Solution:
    def majorityElement(self, nums) -> int:

        mydict = {}

        for x in nums:
            if x not in mydict:
                mydict[x] = 1
            else:
                mydict[x] +=1

        maxfreq = max(mydict.values())

        for keys,values in mydict.items():
            if values == maxfreq:
                return keys
