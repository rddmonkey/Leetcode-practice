class Solution:
    def removeDuplicates(self, s: str):

        testlist = []

        for x in s:
            if testlist != [] and testlist[-1] == x:
                testlist.pop()
            else:

                testlist.append(x)

        return "".join(testlist)
